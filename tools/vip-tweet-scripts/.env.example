# Copy to .env (gitignored) and fill in. The tool reads either var name.
# X API v2 recent-search requires at least the Basic ($) access tier.
TWITTER_BEARER_TOKEN=
# XAPI_BEARER_TOKEN=   # alternative name, same effect
